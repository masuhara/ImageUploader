# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ImageCollectionSample' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ImageCollectionSample

  pod 'NCMB', :git => 'https://github.com/NIFCloud-mbaas/ncmb_ios.git'
  pod 'SVProgressHUD'
  pod 'Kingfisher'

  target 'ImageCollectionSampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ImageCollectionSampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
